<UserControl x:Class="Sasha_Project.Views.SettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
             xmlns:viewmodels="clr-namespace:Sasha_Project.ViewModels">

    <UserControl.DataContext>
        <viewmodels:SettingsViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <ContentControl Grid.Row="1" Content="{Binding SelectedSettingsBaseViewModel}"/>

        <TextBlock Grid.Row="1" FontSize="80">Hello</TextBlock>

        <Button Width="200" Command="{Binding NewSettingsPage}">Switch</Button>
    </Grid>


</UserControl>
